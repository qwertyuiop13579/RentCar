@model IEnumerable<Labs.Models.Clients>
@{
    ViewBag.Title = "Список клиентов";
}

<a asp-action="Create" asp-controller="Client">Добавить клиента</a>

<table class="table">
    <tr><th>Паспорт</th><th>Адрес</th><th>Банк</th><th></th><th></th></tr>
    @foreach (var cl in Model)
    {
        <tr>
            <td>@cl.id_passport</td>
            <td>@cl.id_address</td>
            <td>@cl.id_bank_details</td>
            <td>
                <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@cl.Id">Изменить</a>
                <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@cl.Id">Удалить</a>
            </td>
        </tr>
    }
</table>